private static int fn_A(int a,int b,int c,int d){
  int e=Math.abs(a - c);
  int f=Math.abs(b - d);
  if (f < e) {
    int g=e;
    e=f;
    f=g;
  }
  if (e < (f >> 2)) {
    return f;
  }
  int h=(3 * e) >> 3;
  return (h >> 5) + h + f - (f >> 4) - (f >> 6);
}
