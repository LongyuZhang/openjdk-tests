private static boolean fn_A(byte[] a,int b,int c,byte[] d,int e,int f){
  if (c != f) {
    return false;
  }
  for (int g=b, h=e, i=0; i < c; g++, h++, i++) {
    if (a[g] != d[h]) {
      return false;
    }
  }
  return true;
}
