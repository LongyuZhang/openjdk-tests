private static float fn_A(double a,float[] b){
  a=(int)(a * 10 + 0.5) / 10.0;
  int c=0;
  int d=b.length - 1;
  while (c < d) {
    final int e=(c + d) / 2;
    final double f=b[e];
    if (a == f)     return (float)a;
    if (a < f)     d=e - 1;
 else     c=e + 1;
  }
  double g=9999;
  int h=0;
  for (int e=d - 1; e <= c + 1; ++e)   if (e >= 0 && e < b.length) {
    final double i=Math.abs(b[e] - a);
    if (i < g) {
      g=i;
      h=e;
    }
  }
  return b[h];
}
