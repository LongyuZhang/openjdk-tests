public static float[] fn_A(float[] a,float[] b){
  float[] c=new float[a.length + b.length];
  int d=0;
  for (int e=0; e < a.length; e++)   c[d++]=a[e];
  for (int e=0; e < b.length; e++)   c[d++]=b[e];
  return c;
}
