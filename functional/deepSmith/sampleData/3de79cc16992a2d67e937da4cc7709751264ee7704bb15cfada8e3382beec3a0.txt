public static int fn_A(int a,int b,int c,boolean d){
  if (b > c) {
    int e=c;
    c=b;
    b=e;
  }
  int f=0xFFFFFFFF;
  int g=(f >>> (31 - c)) & (f << b);
  a=a & g;
  if (d) {
    a=a << (31 - c);
    return a >> (b + (31 - c));
  }
  return a >>> b;
}
