public static double[] fn_A(double[] a,double[] b){
  double[] c=new double[a.length + b.length];
  int d=0;
  for (int e=0; e < a.length; e++, d++) {
    c[d]=a[d];
  }
  for (int e=0; e < b.length; e++, d++) {
    c[d]=b[e];
  }
  return c;
}
