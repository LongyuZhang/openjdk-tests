private static long fn_A(long[] a,int b,long[] c,int d,int e,int f){
  int g=64 - f;
  long h=0;
  for (int i=0; i < e; ++i) {
    long j=a[b + i];
    c[d + i]=(j << f) | h;
    h=j >>> g;
  }
  return h;
}
