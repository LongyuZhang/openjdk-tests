public static int fn_A(byte[] a,int b,int c,byte[] d,int e,int f){
  if (a == d && b == e && c == f)   return 0;
  int g=b + c, h=e + f;
  for (int i=b, j=e; i < g && j < h; i++, j++) {
    int k=a[i], l=d[j];
    if (k != l)     return k - l;
  }
  return g - h;
}
