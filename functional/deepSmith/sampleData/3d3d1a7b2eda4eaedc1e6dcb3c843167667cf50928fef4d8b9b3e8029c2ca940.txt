private static void fn_A(int a,int b,int c,String d) throws Exception {
  c=(c < 0) ? c + 256 : c;
  if (c > 1) {
    throw new Exception("Comp. " + d + " at "+ a+ ","+ b+ " should be 0, not "+ c);
  }
}
