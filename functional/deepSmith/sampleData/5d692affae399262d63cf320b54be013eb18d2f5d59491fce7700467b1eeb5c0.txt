public static float[][] fn_A(double a[][]){
  float[][] b=new float[a.length][];
  for (int c=0; c < a.length; c++) {
    b[c]=new float[a[c].length];
    for (int d=0; d < a[c].length; d++) {
      b[c][d]=(float)a[c][d];
    }
  }
  return b;
}
