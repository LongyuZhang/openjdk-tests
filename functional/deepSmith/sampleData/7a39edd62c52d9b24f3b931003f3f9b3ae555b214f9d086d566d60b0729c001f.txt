private static void fn_A(double[][] a,int[] b,int[] c){
  for (int d=b.length; d-- > 0; ) {
    if (b[d] != c[d]) {
      for (int e=b.length; e-- > 0; ) {
        final double f=a[e][b[d]];
        a[e][b[d]]=a[e][c[d]];
        a[e][c[d]]=f;
      }
    }
  }
}
