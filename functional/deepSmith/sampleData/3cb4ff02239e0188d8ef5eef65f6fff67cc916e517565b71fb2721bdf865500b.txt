private static double fn_A(double[] a,double[] b,double[] c){
  double d=0;
  for (int e=0; e < a.length; e++) {
    if (c[e] != 0) {
      d+=Math.sqrt(a[e] * a[e] + b[e] * b[e]);
    }
  }
  return d;
}
