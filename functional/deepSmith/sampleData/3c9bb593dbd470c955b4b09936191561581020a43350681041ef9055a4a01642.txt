private static boolean fn_A(char[] a,char[] b){
  if (a == b)   return true;
  if (a != null && b != null && a.length == b.length) {
    for (int c=a.length; c-- > 0; )     if (a[c] != b[c])     return false;
    return true;
  }
  return false;
}
