private static void fn_A(byte[] a){
  for (int b=0; b < a.length; b++)   a[b]=(byte)((~a[b]) & 0xFF);
  for (int b=a.length - 1; b >= 0; b--) {
    if (a[b] != -1) {
      a[b]++;
      break;
    }
 else {
      a[b]=0;
    }
  }
}
