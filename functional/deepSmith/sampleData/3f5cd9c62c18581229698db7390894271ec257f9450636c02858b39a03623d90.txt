protected final static double fn_A(double[][] a){
  if (a[0].length != 1)   throw new IllegalArgumentException();
  double b=0;
  double c;
  for (int d=0; d < a.length; d++) {
    c=a[d][0];
    b+=c * c;
  }
  return b;
}
