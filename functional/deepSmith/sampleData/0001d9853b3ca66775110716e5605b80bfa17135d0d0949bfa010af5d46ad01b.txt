public static float[] fn_A(final float[] a,final int b,final int c){
  final float[] d=new float[c - b + 1];
  for (int e=b; e <= c; e++) {
    float f=0;
    for (int g=0; g < a.length - e; g++) {
      f+=a[g] * a[g + e];
    }
    d[e - b]=f;
  }
  return d;
}
