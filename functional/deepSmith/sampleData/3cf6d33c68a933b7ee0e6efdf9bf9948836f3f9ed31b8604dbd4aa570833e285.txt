public static boolean fn_A(byte[] a,byte[] b,int c){
  if (a == null || b == null || a.length + c < b.length) {
    return false;
  }
  for (int d=c; d < b.length; d++) {
    if (a[d] != b[d]) {
      return false;
    }
  }
  return true;
}
