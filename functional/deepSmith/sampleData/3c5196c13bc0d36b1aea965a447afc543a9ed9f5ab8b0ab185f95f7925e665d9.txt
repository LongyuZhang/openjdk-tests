public static double[][] fn_A(double[][] a){
  if (a.length == 0)   return new double[0][0];
  double[][] b=new double[a.length][a[0].length];
  for (int c=0; c < a.length; c++) {
    for (int d=0; d < a[0].length; d++) {
      b[c][d]=Math.log(a[c][d]);
    }
  }
  return b;
}
