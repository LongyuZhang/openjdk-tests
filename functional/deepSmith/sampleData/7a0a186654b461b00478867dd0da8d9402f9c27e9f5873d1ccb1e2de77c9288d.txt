public static int fn_A(int a[],int b){
  int c[]=new int[b + 1];
  c[0]=1;
  for (int d=0; d < a.length; d++) {
    for (int e=1; e <= b; e++) {
      if (e >= a[d])       c[e]+=c[e - a[d]];
    }
  }
  return c[b];
}
