public static byte fn_A(int a){
  if (a > 255 || a < 0) {
    throw new IllegalArgumentException("Can only convert non-negative integers between [0,255] to byte: [" + a + "]");
  }
  if (a < 128) {
    return (byte)a;
  }
 else {
    return (byte)(a - 256);
  }
}
