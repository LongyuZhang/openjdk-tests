public static int fn_A(int[][] a){
  int b=Integer.MIN_VALUE;
  for (int c=0; c < a.length; c++) {
    for (int d=0; d < a[0].length; d++) {
      if (a[c][d] < Integer.MAX_VALUE && b < a[c][d])       b=a[c][d];
    }
  }
  return b;
}
