public static double[] fn_A(double a,double b,int c){
  double[] d=new double[c + 2];
  d[0]=a;
  d[d.length - 1]=b;
  double e=(b - a) / (c + 1);
  for (int f=1; f < d.length - 1; f++) {
    d[f]=d[f - 1] + e;
  }
  return d;
}
