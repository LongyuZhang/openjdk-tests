public static int[] fn_A(final int a,final int b){
  if (b < a) {
    throw new IllegalArgumentException("Max: " + b + " less than min: "+ a);
  }
  int[] c=new int[b - a + 1];
  int d=a;
  for (int e=0; e < (b - a + 1); e++) {
    c[e]=d++;
  }
  return c;
}
