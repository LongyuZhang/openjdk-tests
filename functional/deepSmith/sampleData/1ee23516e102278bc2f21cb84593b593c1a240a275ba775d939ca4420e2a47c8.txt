public static File fn_A(String... a){
  if (a.length == 0) {
    return null;
  }
  String b=a[0];
  for (int c=1; c < a.length; c++) {
    b=new File(b,a[c]).getAbsolutePath();
  }
  return new File(b);
}
