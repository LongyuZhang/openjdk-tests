public static double fn_A(double[] a,double[] b){
  if (a.length != b.length) {
    throw new IllegalArgumentException("dimensions don't match");
  }
  double c=0;
  for (int d=0; d < a.length; d++) {
    c+=Math.pow(a[d] - b[d],2);
  }
  return c / a.length;
}
