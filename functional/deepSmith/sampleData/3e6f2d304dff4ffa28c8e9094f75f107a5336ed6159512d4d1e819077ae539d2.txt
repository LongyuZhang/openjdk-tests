public static int fn_A(float[] a){
  if (a.length == 0)   return -1;
  int b=0;
  float c=Float.POSITIVE_INFINITY;
  for (int d=a.length; d-- > 0; )   if (a[d] < c) {
    c=a[d];
    b=d;
  }
  return b;
}
