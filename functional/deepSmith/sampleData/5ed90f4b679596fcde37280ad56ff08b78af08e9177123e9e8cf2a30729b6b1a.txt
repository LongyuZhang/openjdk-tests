public static int fn_A(float[] a){
  if (a == null || a.length == 0)   throw new IllegalArgumentException("Input to indexMax() is an empty array");
  float b=a[0];
  int c=0;
  for (int d=1; d < a.length; ++d) {
    if (a[d] > b) {
      b=a[d];
      c=d;
    }
  }
  return c;
}
