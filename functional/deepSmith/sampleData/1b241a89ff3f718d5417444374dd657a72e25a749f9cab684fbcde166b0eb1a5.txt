public static double[][] fn_A(double[][] a){
  int b=a.length;
  double[][] c=new double[b][b];
  for (int d=0; d < b; d++) {
    for (int e=0; e <= d; e++) {
      c[d][e]=a[d][e];
      c[e][d]=a[d][e];
    }
  }
  return c;
}
