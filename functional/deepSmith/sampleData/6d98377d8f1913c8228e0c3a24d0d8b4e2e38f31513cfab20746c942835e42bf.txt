public static int fn_A(byte[] a,int b,int c){
  int d=0;
  int e=0;
  int f=b + c - 1;
  for (int g=b; g < f; g++) {
    int h=a[g];
    d|=(h & 0x7F) << e;
    e+=7;
  }
  int h=a[f];
  return d | (h << e);
}
