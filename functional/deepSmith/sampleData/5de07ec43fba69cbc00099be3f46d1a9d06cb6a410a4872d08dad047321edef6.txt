public static Boolean fn_A(Object a){
  if (a instanceof Boolean) {
    return (Boolean)a;
  }
  if (a instanceof String) {
    return Boolean.valueOf((String)a);
  }
  if (a instanceof Integer) {
    return (Integer)a > 0 ? Boolean.TRUE : Boolean.FALSE;
  }
  return null;
}
