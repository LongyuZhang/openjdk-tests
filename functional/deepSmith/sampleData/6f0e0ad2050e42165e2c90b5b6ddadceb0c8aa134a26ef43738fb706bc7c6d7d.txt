private static double fn_A(double a){
  double b=0.0001;
  double c=0.0;
  double d=a;
  double e=0.0;
  for (int f=0; f < 100; f++) {
    e=(c + d) / 2;
    double g=e * e;
    if (Math.abs(a - g) < b) {
      return e;
    }
    if (g < a)     c=e;
 else     d=e;
  }
  return e;
}
