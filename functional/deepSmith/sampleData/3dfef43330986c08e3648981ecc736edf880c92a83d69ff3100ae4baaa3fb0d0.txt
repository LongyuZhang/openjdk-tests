private static ClassLoader[] fn_A(ClassLoader[] a){
  ClassLoader[] b=new ClassLoader[a.length];
  for (int c=0; c < a.length; c++) {
    ClassLoader d=a[c];
    if (d == null) {
      throw new RuntimeException("parent[" + c + "] is null");
    }
    b[c]=d;
  }
  return b;
}
