public static float[] fn_A(float[] a){
  float[] b=new float[a.length];
  float c;
  b[0]=1;
  for (int d=1; d < a.length; d++) {
    c=0;
    for (int e=0; e < d; e++) {
      c+=a[e];
    }
    b[d]=a[d] / (c / d);
  }
  return b;
}
