private static int fn_A(int a,int b,int c){
  int[] d=new int[b + 1];
  d[0]=1;
  for (int e=1; e <= b; ++e) {
    d[e]=1;
    for (int f=e - 1; f > 0; --f) {
      d[f]=(d[f] + d[f - 1]) % c;
    }
  }
  for (int e=b + 1; e <= a; ++e) {
    for (int f=b; f > 0; --f) {
      d[f]=(d[f] + d[f - 1]) % c;
    }
  }
  int g=0;
  for (int e=0; e <= b; g=(g + d[e++]) % c) {
  }
  return g;
}
