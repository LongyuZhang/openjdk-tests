public static int fn_A(int[] a){
  int b=0;
  for (int c=31; c >= 0; c--) {
    int d=0;
    for (int e=0; e < a.length; e++) {
      d=d + (a[e] >>> c & 1);
    }
    if (d > a.length / 2) {
      b+=(1 << c);
    }
  }
  return b;
}
