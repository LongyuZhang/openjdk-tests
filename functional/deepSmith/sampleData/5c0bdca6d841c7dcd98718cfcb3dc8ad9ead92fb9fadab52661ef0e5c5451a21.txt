private static int fn_A(int[] a,int[] b){
  int c=0;
  int d=0;
  int e=0;
  while (d < a.length && e < b.length) {
    c+=a[d];
    c-=b[e];
    d++;
    e++;
  }
  if (d < a.length) {
    c+=a[d];
  }
  if (e < b.length)   c-=b[e];
  return c;
}
