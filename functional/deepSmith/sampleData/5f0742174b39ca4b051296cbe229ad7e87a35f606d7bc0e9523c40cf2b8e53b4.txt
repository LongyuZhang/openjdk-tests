public static void fn_A(double[] a,int b){
  System.out.println("The last " + b + " elements of the output array are as follows:");
  for (int c=0; c < a.length; c++) {
    if (c > a.length - 1 - b)     System.out.println("outputArray[" + c + "]= "+ a[c]);
  }
}
