private static void fn_A(byte[] a,int b,int[] c){
  int d, e;
  for (d=0; d < 8; d++) {
    long f=c[2 * d] + (((long)c[2 * d + 1]) << 28);
    for (e=0; e < 7; e++) {
      a[b + 7 * d + e]=(byte)f;
      f>>=8;
    }
  }
}
