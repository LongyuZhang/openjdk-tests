public static void fn_A(int[] a){
  for (int b=0; b < a.length - 1; b++) {
    int c=b;
    int d=a[b];
    for (int e=b + 1; e < a.length; e++) {
      if (a[e] < d) {
        c=e;
        d=a[e];
      }
    }
    if (c != b) {
      a[c]=a[b];
      a[b]=d;
    }
  }
}
