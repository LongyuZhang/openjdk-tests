public static void fn_A(int[] a){
  for (int b=0; b < a.length - 1; b++) {
    for (int c=1; c < a.length - 1; c++) {
      if (a[c - 1] < a[c]) {
        int d=a[c - 1];
        a[c - 1]=a[c];
        a[c]=d;
      }
    }
  }
}
