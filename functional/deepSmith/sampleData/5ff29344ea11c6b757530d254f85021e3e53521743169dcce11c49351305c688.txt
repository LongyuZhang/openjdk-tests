private static void fn_A(int[] a,int b,int c,int d){
  for (int e=b, f=c + 1; e < d; e++) {
    if (e == f)     f++;
    if (a[e] > a[f]) {
      int g=a[e];
      a[e]=a[f];
      a[f]=g;
    }
  }
}
