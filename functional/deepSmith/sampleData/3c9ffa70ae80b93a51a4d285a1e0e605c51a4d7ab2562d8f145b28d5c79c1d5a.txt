public static boolean fn_A(String[][] a,String b){
  for (int c=0; c < a.length; c++) {
    int d=0;
    for (int e=0; e < a[c].length; e++) {
      if (a[c][e] == b)       d++;
    }
    if (d == 3)     return true;
  }
  return false;
}
