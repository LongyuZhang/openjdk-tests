private static boolean fn_A(Object[] a,Class... b){
  if (a == null) {
    return b.length == 0;
  }
  if (a.length != b.length) {
    return false;
  }
  for (int c=0; c < a.length; c++) {
    if (!b[c].isAssignableFrom(a[c].getClass())) {
      return false;
    }
  }
  return true;
}
