public static int fn_A(int a){
  if (a <= 1)   return 1;
  int[] b=new int[a + 1];
  b[1]=b[0]=1;
  for (int c=2; c <= a; c++) {
    for (int d=0; d < c; d++)     b[c]+=b[d] * b[c - d - 1];
  }
  return b[a];
}
