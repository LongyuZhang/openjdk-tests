public static double fn_A(double[] a,double[] b){
  if (a.length != b.length)   throw new IllegalArgumentException("mismatching sizes");
  double c=0.0;
  for (int d=0; d < a.length; d++) {
    double e=a[d] - b[d];
    c+=e * e;
  }
  return c;
}
