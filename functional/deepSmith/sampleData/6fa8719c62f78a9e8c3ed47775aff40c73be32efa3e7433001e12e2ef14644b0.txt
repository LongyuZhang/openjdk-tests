public static double[] fn_A(double[] a){
  double[] b=new double[a.length];
  for (int c=0; c < b.length; c++)   b[c]=a[c];
  double d=0.0;
  for (int c=0; c < b.length; c++)   if (Math.abs(b[c]) > d)   d=Math.abs(b[c]);
  if (d != 0.0)   for (int c=0; c < b.length; c++)   b[c]/=d;
  return b;
}
