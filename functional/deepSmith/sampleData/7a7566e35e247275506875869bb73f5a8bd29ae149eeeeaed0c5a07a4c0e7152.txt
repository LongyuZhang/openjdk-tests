private static final String fn_A(double a){
  int b=(int)(a * 100.0);
  if (a >= 1) {
    if ((b % 100) != 0)     return b / 100f + "%";
    return (int)a + "%";
  }
 else   if (a < 1 && a > 0) {
    return b / 100f + "%";
  }
  return a + "%";
}
