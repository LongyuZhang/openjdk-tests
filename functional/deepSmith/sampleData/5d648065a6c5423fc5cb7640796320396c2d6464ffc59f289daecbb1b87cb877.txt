private static void fn_A(double a[],double b[],double c){
  double d;
  int e=a.length - 1;
  d=a[e];
  for (int f=0; f <= e; ++f)   b[f]=d;
  for (int g=1; g <= e; ++g) {
    b[e]=b[e] * c + a[g];
    for (int f=1; f <= e - g; ++f)     b[e - f]=b[e - f] * c + b[e - (f - 1)];
  }
}
