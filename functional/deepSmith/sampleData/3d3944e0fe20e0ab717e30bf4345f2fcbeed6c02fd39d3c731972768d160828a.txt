public static boolean fn_A(double[] a){
  int b=a.length;
  int c=0;
  for (int d=1; d < b - 1; d++) {
    if (a[d - 1] < a[d] && a[d + 1] < a[d]) {
      c++;
      if (c > 2)       return false;
    }
  }
  return (c == 2);
}
