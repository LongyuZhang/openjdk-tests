public static boolean fn_A(long[] a,long b,int c,long d){
  int e=1;
  long f=d;
  int g=0;
  while (g < a.length && e <= b) {
    if (a[g] <= f) {
      f=f - a[g];
      g++;
    }
 else {
      e++;
      f=d;
    }
  }
  if (g == a.length) {
    return true;
  }
 else {
    return false;
  }
}
