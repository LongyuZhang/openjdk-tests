public static float[][] fn_A(float[][] a,float[] b){
  float[][] c=new float[a.length][];
  for (int d=0; d < a.length; d++) {
    int e=a[d].length;
    c[d]=new float[e];
    for (int f=0; f < e; f++) {
      c[d][f]=a[d][f] + b[f];
    }
  }
  return c;
}
