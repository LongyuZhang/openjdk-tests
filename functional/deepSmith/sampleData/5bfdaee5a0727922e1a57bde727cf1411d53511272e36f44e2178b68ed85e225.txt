public static int fn_A(char a[],int b,int c,int d,int e){
  int f=e;
  int g=b + c;
  while (b < g) {
switch (a[b++]) {
case '\t':
      f=(f + d) / d * d;
    break;
default :
  f++;
}
}
return f;
}
