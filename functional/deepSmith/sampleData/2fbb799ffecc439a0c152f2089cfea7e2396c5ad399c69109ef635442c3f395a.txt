static void fn_A(double a){
  final int b=50;
  System.out.print("\r[");
  for (int c=0; c < b; c++) {
    System.out.print(c <= (int)(a / (100 / b)) ? "." : " ");
  }
  System.out.print("] " + (int)a + "%");
  System.out.flush();
}
