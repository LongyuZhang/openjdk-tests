public static double[][] fn_A(int a,int b,int c){
  double[][] d=new double[a][b];
  for (int e=0; e < d.length; e++) {
    for (int f=0; f < d[e].length; f++) {
      d[e][f]=(int)(Math.random() * c + 1);
    }
  }
  return d;
}
