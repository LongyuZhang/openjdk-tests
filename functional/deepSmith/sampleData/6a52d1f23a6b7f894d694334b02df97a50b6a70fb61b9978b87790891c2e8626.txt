public static double fn_A(long a,long b){
  if (b == 0 || b == a) {
    return 1.0;
  }
  if (b > a / 2.0) {
    b=a - b;
  }
  double c=1.0;
  long d=a - b + 1;
  for (long e=b; e > 0; ) {
    c*=((double)d++) / (double)(e--);
  }
  return c;
}
