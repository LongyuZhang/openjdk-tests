public static void fn_A(int[] a,int[] b){
  if (a.length != b.length) {
    throw new IllegalArgumentException(String.format("Arrays have different length: x[%d], y[%d]",a.length,b.length));
  }
  for (int c=0; c < a.length; c++) {
    int d=a[c];
    a[c]=b[c];
    b[c]=d;
  }
}
