public static int fn_A(int a){
  int[][] b=new int[a + 1][a + 1];
  for (int c=1; c <= a; ++c) {
    b[1][c]=1;
    b[c][1]=1;
  }
  for (int d=2; d <= a; ++d) {
    for (int c=2; c <= a; ++c) {
      b[d][c]=b[d - 1][c] + b[d][c - 1];
    }
  }
  return b[a][a];
}
