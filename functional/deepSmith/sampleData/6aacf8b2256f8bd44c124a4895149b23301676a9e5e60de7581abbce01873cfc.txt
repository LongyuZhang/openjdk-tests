private static final void fn_A(int a,byte[] b,int c,int d){
  if (d > 4) {
    throw new IllegalArgumentException("Cannot handle more than 4 bytes");
  }
  for (int e=d - 1; e >= 0; e--) {
    b[c + e]=(byte)(a & 0xff);
    a>>>=8;
  }
}
