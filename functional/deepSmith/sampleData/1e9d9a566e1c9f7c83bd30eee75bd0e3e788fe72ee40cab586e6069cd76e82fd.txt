private static boolean fn_A(int[] a,int b,int c,int d){
  int e=1;
  int f=d;
  int g=0;
  while (g < a.length && e <= b) {
    if (a[g] <= f) {
      f-=a[g];
      g++;
    }
 else {
      e++;
      f=d;
    }
  }
  return g == a.length;
}
