public static float[][] fn_A(final float[][] a,final float[][] b){
  int c, d, e;
  final int f=a[0].length;
  final int g=a.length;
  final int h=b[0].length;
  double i;
  final float[][] j=new float[f][h];
  for (c=0; c < f; c++)   for (d=0; d < h; j[c][d]=(float)i, d++)   for (i=0, e=0; e < g; i+=((double)a[e][c]) * ((double)b[e][d]), e++)   ;
  return j;
}
