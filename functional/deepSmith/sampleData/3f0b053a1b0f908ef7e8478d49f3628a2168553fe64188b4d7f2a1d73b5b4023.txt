private static void fn_A(int[] a){
  for (int b=0; b < a.length - 1; b++) {
    for (int c=b + 1; c < a.length; c++) {
      if (a[b] < a[c]) {
        int d=a[b];
        a[b]=a[c];
        a[c]=d;
      }
    }
  }
}
