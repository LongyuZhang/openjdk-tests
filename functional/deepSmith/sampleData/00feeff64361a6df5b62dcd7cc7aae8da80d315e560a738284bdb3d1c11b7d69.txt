public static double[] fn_A(float[] a){
  int b=a.length;
  int c=(b * (b - 1)) / 2;
  double[] d=new double[c];
  int e=0;
  for (int f=0; f < b; f++) {
    for (int g=f + 1; g < b; g++) {
      d[e++]=(a[f] + a[g]) / 2.0f;
    }
  }
  return d;
}
