static final int fn_A(int a[]){
  if (a.length == 3) {
    int b=a[0];
    int c=a[1];
    int d=a[2];
    if (b < c) {
      return (c < d ? d : c);
    }
 else {
      return (b < d ? d : b);
    }
  }
  int e=a[0];
  for (int f=1; f < a.length; f++)   if (e < a[f])   e=a[f];
  return e;
}
