private static void fn_A(long[] a,final int b,final int c){
  int d;
  long e;
  for (d=0; d < 16; d++) {
    a[d + b]+=(1L << 16);
    e=a[d + b] >> 16;
    a[(d + 1) * ((d < 15) ? 1 : 0) + b]+=e - 1 + 37 * (e - 1) * ((d == 15) ? 1 : 0);
    a[d + b]-=(e << 16);
  }
}
