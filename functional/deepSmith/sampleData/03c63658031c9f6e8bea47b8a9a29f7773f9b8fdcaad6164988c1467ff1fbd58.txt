public static long fn_A(byte[] a,int b,int c){
  if (c <= 0) {
    throw new NumberFormatException();
  }
  long d=0;
  do {
    int e=a[b] - '0';
    if (e < 0 || e > 9) {
      throw new NumberFormatException();
    }
    d=d * 10 + e;
    b++;
    c--;
  }
 while (c > 0);
  return d;
}
