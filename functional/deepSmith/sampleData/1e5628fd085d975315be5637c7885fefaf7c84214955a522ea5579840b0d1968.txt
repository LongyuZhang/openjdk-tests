public static File fn_A(String a){
  String b=System.getProperty("user.home",".");
  File c;
  String d=System.getenv("APPDATA");
  if (d != null) {
    c=new File(d,"." + a + "/");
  }
 else {
    c=new File(b,"." + a + "/");
  }
  return c;
}
