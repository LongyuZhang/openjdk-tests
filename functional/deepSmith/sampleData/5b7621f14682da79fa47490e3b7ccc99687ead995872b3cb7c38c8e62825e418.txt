private static int[] fn_A(byte[] a){
  int[] b=new int[256];
  for (int c=0; c < b.length; c++) {
    b[c]=a.length;
  }
  for (int c=0; c < a.length - 1; c++) {
    b[a[c] & 0xff]=a.length - c - 1;
  }
  return b;
}
