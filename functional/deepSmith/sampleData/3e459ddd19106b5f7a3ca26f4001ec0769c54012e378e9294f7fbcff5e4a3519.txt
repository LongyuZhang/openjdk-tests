static final float fn_A(float[] a,float[] b){
  assert(a.length == b.length);
  double c=0;
  for (int d=0; d < a.length; d++) {
    double e=(double)a[d] * Math.log(a[d] / b[d]);
    c+=e;
  }
  return (float)c;
}
