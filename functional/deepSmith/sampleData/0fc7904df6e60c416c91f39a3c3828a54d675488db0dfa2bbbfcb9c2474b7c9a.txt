public static int fn_A(byte[] a,int b){
  int c=0;
  if (a == null || a.length == 0) {
    return c;
  }
  for (int d=0; d < a.length; d++) {
    c+=(int)((a[d] & 0xFF) << (8 * (a.length - d - 1)));
  }
  return c;
}
