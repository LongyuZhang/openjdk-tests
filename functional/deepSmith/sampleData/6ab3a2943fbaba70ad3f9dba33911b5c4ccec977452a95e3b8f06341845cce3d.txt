private static int fn_A(double... a){
  int b=0;
  double c=Math.signum(a[0]);
  double d;
  for (int e=0; e < a.length; e++) {
    if (a[e] == 0) {
      continue;
    }
    d=Math.signum(a[e]);
    if (d != c) {
      b++;
    }
    c=d;
  }
  return b;
}
