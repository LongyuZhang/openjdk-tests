private static float[] fn_A(final float[] a,final float[] b,final float[] c){
  c[0]=(a[1] * b[2]) - (a[2] * b[1]);
  c[1]=(-a[0] * b[2]) + (a[2] * b[0]);
  c[2]=(a[0] * b[1]) - (a[1] * b[0]);
  return c;
}
