public static float fn_A(float[] a,int b){
  float c=a[b];
  int d;
  float e=c * 0.9f;
  for (d=1; d <= b; d++) {
    if (a[d] >= e) {
      float f=(e - a[d - 1]) / (a[d] - a[d - 1]);
      return (d - 1 + f);
    }
  }
  return -1;
}
