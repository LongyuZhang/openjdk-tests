public static int fn_A(int[] a){
  if (a == null || a.length == 0) {
    return Integer.MIN_VALUE;
  }
  int[] b=new int[32];
  int c=0;
  for (int d=0; d < 32; d++) {
    for (int e=0; e < a.length; e++) {
      b[d]+=((a[e] >> d) & 0x01);
    }
    c|=((b[d] % 3) << d);
  }
  return c;
}
