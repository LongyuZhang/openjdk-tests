private static long fn_A(long[] a,int b,long[] c,int d,int e,int f){
  int g=64 - f;
  long h=0;
  int i=e;
  while (--i >= 0) {
    long j=c[d + i];
    a[b + i]^=(j >>> f) | h;
    h=j << g;
  }
  return h;
}
