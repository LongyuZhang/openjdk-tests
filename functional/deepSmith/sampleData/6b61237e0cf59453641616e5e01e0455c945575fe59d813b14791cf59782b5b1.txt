static void fn_A(final float[] a,int b,final float[] c,float[] d,int e){
  int f, g;
  float h;
  float[] i;
  int j;
  for (f=b - 1; f >= 0; f--) {
    i=a;
    j=f;
    h=0;
    for (g=b - 1; g > f; g--) {
      h+=i[j + g * b] * d[e + g];
    }
    h=c[f] - h;
    d[e + f]=h;
  }
}
