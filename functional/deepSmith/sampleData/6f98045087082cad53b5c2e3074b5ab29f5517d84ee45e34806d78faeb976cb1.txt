public static int fn_A(int[] a,int b){
  if (b <= 1)   return 0;
  int c=a.length;
  int d=0, e=0, f=1;
  for (int g=0; g < c; g++) {
    f*=a[g];
    while (f >= b)     f/=a[d++];
    e+=g - d + 1;
  }
  return e;
}
