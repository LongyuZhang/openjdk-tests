private static final int[] fn_A(byte[] a){
  int[] b=new int[a.length];
  int c=0;
  for (int d=1; d < a.length; d++) {
    while (c > 0 && a[c] != a[d]) {
      c=b[c - 1];
    }
    if (a[c] == a[d]) {
      c++;
    }
    b[d]=c;
  }
  return b;
}
