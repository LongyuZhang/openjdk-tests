private static int[] fn_A(){
  int[] a=new int[256];
  for (int b=0; b < 256; b++) {
    int c=b;
    for (int d=8; --d >= 0; ) {
      if ((c & 1) != 0)       c=0xedb88320 ^ (c >>> 1);
 else       c>>>=1;
    }
    a[b]=c;
  }
  return a;
}
