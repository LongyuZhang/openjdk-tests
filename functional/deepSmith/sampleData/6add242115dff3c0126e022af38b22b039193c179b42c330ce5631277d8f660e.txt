protected static Class<?> fn_A(final Object a){
  Class<?> b=void.class;
  if (a instanceof Byte || a instanceof Short || a instanceof Character|| a instanceof Integer) {
    b=Integer.class;
  }
 else   if (a instanceof Long) {
    b=Long.class;
  }
  return b;
}
