public static int fn_A(int[][] a,int b,int c,int d){
  int e=0, f=c - 1, g=0;
  while (e < b && f >= 0) {
    if (a[e][f] < d) {
      g+=(f + 1);
      ++e;
    }
 else {
      --f;
    }
  }
  return g;
}
