protected static final void fn_A(int[] a,int[] b,int c,int d){
  for (int e=c + 1; e <= d; ++e) {
    int f=a[e];
    int g=b[e];
    int h=e - 1;
    while (h >= c && a[h] > f) {
      a[h + 1]=a[h];
      b[h + 1]=b[h];
      h--;
    }
    a[h + 1]=f;
    b[h + 1]=g;
  }
}
