public static boolean fn_A(Object[] a,Object[] b){
  a:   for (int c=0; c < b.length; c++) {
    for (int d=0; d < a.length; d++) {
      if (b[c] == a[d] || b[c].equals(a[d])) {
        continue a;
      }
    }
    return false;
  }
  return true;
}
