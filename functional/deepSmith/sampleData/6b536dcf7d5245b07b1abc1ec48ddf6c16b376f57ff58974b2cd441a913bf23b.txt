public static double fn_A(double a,double b,double c){
  if (a <= 0) {
    return 0;
  }
  if (a >= 1) {
    return 1;
  }
  if (a <= b) {
    return a * 25 / b;
  }
  if (a <= c) {
    return 25 + (a - b) * (75 - 25) / (c - b);
  }
  return 75 + (a - c) * (100 - 75) / (1 - c);
}
