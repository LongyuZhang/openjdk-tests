public static double[] fn_A(int a,int b,int c){
  int d=a + b;
  double e=0.0;
  if (d > 0)   e=(double)a / (double)d;
  double f=(double)a / (double)c;
  double g=2.0f * e * f / (e + f);
  double[] h=new double[3];
  h[0]=e;
  h[1]=f;
  h[2]=g;
  return h;
}
