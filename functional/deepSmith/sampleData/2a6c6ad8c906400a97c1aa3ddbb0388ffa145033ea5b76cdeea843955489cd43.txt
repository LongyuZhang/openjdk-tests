static long fn_A(int[] a){
  int b=a.length;
  int c=-1;
  for (int d=0; d < b; d++) {
    if ((c == -1) || (a[d] > a[c]))     c=d;
  }
  int e=-1;
  for (int f=0; f < b; f++) {
    if ((f != c) && ((e == -1) || (a[f] > a[e])))     e=f;
  }
  return (long)a[c] * a[e];
}
