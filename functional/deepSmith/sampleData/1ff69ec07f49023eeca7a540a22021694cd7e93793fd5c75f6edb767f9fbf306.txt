static int fn_A(char a,char[][] b,int c){
  int d=0;
  int e;
  if (a < b[0][0] || a > b[c][1])   return 0;
  while (c >= d) {
    e=(d + c) / 2;
    if (a > b[e][1])     d=e + 1;
 else     if (a < b[e][0])     c=e - 1;
 else     return 1;
  }
  return 0;
}
