private static int fn_A(int[] a,int[] b,int c){
  long d=0L;
  for (; ; ) {
    c--;
    if (c < 0)     break;
    d=(a[c] & 0xFFFFFFFF) - (b[c] & 0xFFFFFFFF) + (d >> 32);
    a[c]=((int)d);
  }
  return (int)(d >> 32);
}
