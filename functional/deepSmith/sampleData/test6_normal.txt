public final static int[] fn_A(int[] a,int[] b){
  int[] c=new int[a.length + b.length];
  for (int d=0; d < a.length; d++) {
    c[d]=a[d];
  }
  for (int d=0; d < b.length; d++) {
    c[a.length + d]=b[d];
  }
  return c;
}
