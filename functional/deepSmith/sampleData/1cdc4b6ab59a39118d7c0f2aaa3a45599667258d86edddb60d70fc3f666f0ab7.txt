public static int[] fn_A(int[] a,int b){
  if (a.length == 1 && a[0] == 24) {
    return a;
  }
  int c[]=new int[a.length];
  for (int d=0; d < a.length; d++) {
    c[d]=(a[d] + b) % 12;
  }
  return c;
}
