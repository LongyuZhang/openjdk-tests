public static int fn_A(byte[] a,int b,int c,int d,int e){
  int f=a[b];
  for (int g=0; g < c; g++) {
    int h=b + g * e;
    int i=h + d;
    for (; h < i; h++) {
      int j=a[h];
      if (j > f)       f=j;
    }
  }
  return f;
}
