public static int fn_A(int[][] a){
  if (a.length == 0)   throw new IllegalArgumentException("Input array must have a length greater than 0");
  int b=Integer.MAX_VALUE;
  for (int c=0; c < a.length; c++)   for (int d=0; d < a[c].length; d++)   if (a[c][d] < b)   b=a[c][d];
  return b;
}
