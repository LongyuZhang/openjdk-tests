public static double fn_A(int[] a,int[] b,int c,int d,int e,double[][] f,double g){
  double h=b[e];
  double i=b[d];
  double j=a[d];
  double k=a[e];
  double l=f[e][c] * (g + h) * (g + j);
  double m=f[d][c] * (g + i) * (g + k);
  double n=l / m;
  return n;
}
