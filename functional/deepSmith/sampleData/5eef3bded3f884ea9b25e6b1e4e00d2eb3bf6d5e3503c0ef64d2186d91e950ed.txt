protected static int fn_A(int[] a,int b){
  int c=a.length;
  int d=0, e=c - 1;
  while (d <= e) {
    int f=d + (e - d) / 2;
    if (a[f] == b && (f == c - 1 || a[f + 1] > b)) {
      return f;
    }
 else     if (a[f] > b)     e=f - 1;
 else     d=f + 1;
  }
  return -1;
}
