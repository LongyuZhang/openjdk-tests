private static int fn_A(final int a){
  final boolean b=(a >>> 7 ^ a >>> 6 ^ a >>> 5 ^ a >>> 4 ^ a >>> 3 ^ a >>> 2 ^ ((a >>> 1) & 0x01)) == 0;
  if (b) {
    return a | 0x01;
  }
 else {
    return a & 0xFE;
  }
}
