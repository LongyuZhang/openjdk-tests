public static void fn_A(float[] a,int b,int c,float[] d,int e,int f,int g,int h){
  for (int i=0; i < g; i++) {
    int j=b + i * c;
    int k=e + i * f;
    int l=j + h;
    for (; j < l; j++, k++) {
      d[k]=Math.abs(a[j]);
    }
  }
}
