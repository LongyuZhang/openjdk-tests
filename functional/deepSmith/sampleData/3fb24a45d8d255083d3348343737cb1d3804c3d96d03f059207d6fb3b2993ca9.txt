public static float fn_A(byte[] a,int b,int c){
  float d=0;
  int e=c;
  for (int f=b; f <= c; f++) {
    if (a[f] == '.')     e=f;
 else     if (f < e)     d+=(a[f] - 48) * Math.pow(10,(c - f - 1));
 else     d+=(a[f] - 48) * Math.pow(10,(c - f));
  }
  d/=Math.pow(10,(c - e));
  return d;
}
