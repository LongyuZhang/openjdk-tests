private static boolean fn_A(Class<?>[] a,boolean b,Class<?>[] c,boolean d){
  if (b != d) {
    return d;
  }
  int e=0;
  for (; e < a.length && e < c.length; e++) {
    if (!c[e].isAssignableFrom(a[e])) {
      return false;
    }
  }
  for (; e < a.length; e++) {
    if (!c[c.length - 1].isAssignableFrom(a[e])) {
      return false;
    }
  }
  for (; e < c.length; e++) {
    if (!c[e].isAssignableFrom(a[a.length - 1])) {
      return false;
    }
  }
  return true;
}
