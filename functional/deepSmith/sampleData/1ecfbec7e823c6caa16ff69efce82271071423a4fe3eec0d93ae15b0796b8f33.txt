public static float[] fn_A(final float[] a,final float b){
  final float[] c=new float[a.length - 1];
  int d=0;
  for (int e=0; e < a.length; e++) {
    if (e == b) {
      d=-1;
    }
 else {
      c[e + d]=a[e];
    }
  }
  return c;
}
