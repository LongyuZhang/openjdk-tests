private static String[] fn_A(Object... a){
  final String[] b=new String[a.length];
  for (int c=0; c < a.length; c++) {
    b[c]=a[c] == null ? null : a[c].getClass().getName() + ":" + a[c].getClass().getClassLoader();
  }
  return b;
}
