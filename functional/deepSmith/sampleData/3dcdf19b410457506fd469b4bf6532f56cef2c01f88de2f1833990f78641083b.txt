public static double fn_A(double a,double[] b,int c) throws ArithmeticException {
  int d=0;
  double e=b[d++];
  double f=0.0;
  int g=c - 1;
  double h;
  do {
    h=f;
    f=e;
    e=a * f - h + b[d++];
  }
 while (--g > 0);
  return (0.5 * (e - h));
}
