protected static int fn_A(byte[] a,int b,int c) throws IOException {
  if (c > 4)   throw new RuntimeException("len (" + c + ") > 4");
  int d=0;
  for (int e=0; e < c; e++) {
    d=(d << 8) + (a[b + c - 1 - e] & 0xff);
  }
  return d;
}
