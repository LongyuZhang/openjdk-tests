private static final boolean fn_A(byte[] a,byte[] b){
  if (a == b) {
    return (true);
  }
  int c=a.length;
  if (c != b.length) {
    return (false);
  }
  for (int d=0; d < c; d++) {
    if (a[d] != b[d]) {
      return (false);
    }
  }
  return (true);
}
