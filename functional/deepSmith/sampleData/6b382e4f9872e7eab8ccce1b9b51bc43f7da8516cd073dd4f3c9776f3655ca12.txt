public static UUID fn_A(byte[] a){
  long b=0;
  long c=0;
  for (int d=0; d < 8; d++) {
    b=(b << 8) | (a[d] & 0xff);
  }
  for (int d=8; d < 16; d++) {
    c=(c << 8) | (a[d] & 0xff);
  }
  return new UUID(b,c);
}
