public static int fn_A(int a){
  int b=0;
  int c=1;
  while (a >= c) {
    b+=a / (c * 10) * c;
    int d=a / c % 10;
    if (d == 2) {
      b+=a % c + 1;
    }
 else     if (d > 2) {
      b+=c;
    }
    c*=10;
  }
  return b;
}
