public static void fn_A(double[][] a,double[] b,double[] c){
  for (int d=0; d < a.length; d++) {
    for (int e=0; e < a[d].length; e++) {
      a[d][e]=a[d][e] - b[e];
      if (!Double.isInfinite(1.0 / c[e])) {
        a[d][e]/=c[e];
      }
    }
  }
}
