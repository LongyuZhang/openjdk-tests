public static void fn_A(final double a,final double b,final double c,final String d){
  if (c < a || c > b) {
    throw new IndexOutOfBoundsException("The parameter '" + d + "' must be between '"+ a+ "' and '"+ b+ "' but is '"+ c+ "'.");
  }
}
