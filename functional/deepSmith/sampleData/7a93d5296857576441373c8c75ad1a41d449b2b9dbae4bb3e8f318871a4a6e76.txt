private static int[] fn_A(int[] a,int[] b){
  if (a.length != b.length)   throw new IllegalArgumentException();
  int[] c=new int[a.length];
  for (int d=0; d < a.length; d++) {
    for (int e=0; e < b.length; e++)     c[(d + e) % a.length]+=a[d] * b[e];
  }
  return c;
}
