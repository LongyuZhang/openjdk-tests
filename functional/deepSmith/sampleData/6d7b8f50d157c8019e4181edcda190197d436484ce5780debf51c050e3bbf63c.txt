private static byte fn_A(byte[] a,int b){
  int c=0;
  for (int d=b; d < a.length; d++) {
    int e=(int)a[d] & 0xFF;
    int f=c * 256 + e;
    a[d]=(byte)(f / 58);
    c=f % 58;
  }
  return (byte)c;
}
