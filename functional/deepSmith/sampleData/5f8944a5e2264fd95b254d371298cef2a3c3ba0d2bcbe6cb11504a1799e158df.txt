public static int fn_A(int a,int b){
  if (a >= b || a < -b) {
    throw new IllegalArgumentException("Dimension " + a + " is invalid for rank "+ b+ " tensor.");
  }
  if (a < 0) {
    a+=b;
  }
  return a;
}
