public static void fn_A(final long a,final long b,final long c){
  if ((a | b | (a + b)| (c - (a + b))) < 0) {
    throw new IllegalArgumentException("reqOffset: " + a + ", reqLength: "+ b+ ", (reqOff + reqLen): "+ (a + b)+ ", allocSize: "+ c);
  }
}
