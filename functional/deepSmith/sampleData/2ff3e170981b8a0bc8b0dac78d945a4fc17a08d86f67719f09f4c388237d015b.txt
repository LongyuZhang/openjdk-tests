private static float[] fn_A(float[] a,float[] b){
  float[] c=new float[2];
  c[0]=a[0];
  c[1]=b[0];
  for (int d=1; d < a.length; d++) {
    if (a[d] < c[0]) {
      c[0]=a[d];
    }
    if (b[d] < c[1]) {
      c[1]=b[d];
    }
  }
  return c;
}
