public static byte[] fn_A(byte[] a,int b){
  byte[] c=new byte[a.length - a.length / 4];
  int d=0;
  for (int e=0; e < a.length; e++) {
    if ((e + 1) % 4 == 0) {
      d++;
    }
 else {
      c[e - d]=a[e];
    }
  }
  return c;
}
