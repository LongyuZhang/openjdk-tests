public static int[] fn_A(int a,int b){
  int[] c=new int[(int)((a + 1) * Math.random())];
  for (int d=0; d < c.length; d++) {
    c[d]=(int)((b + 1) * Math.random() - (int)(b * Math.random()));
  }
  return c;
}
