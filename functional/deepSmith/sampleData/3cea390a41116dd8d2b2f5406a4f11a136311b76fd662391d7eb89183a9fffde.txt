protected static void fn_A(int[] a,int b,int c){
  if (b < c) {
    for (int d=b; d <= c - 1; d++) {
      int e=d;
      for (int f=d + 1; f <= c; f++)       if (a[f] < a[e])       e=f;
      if (e != d) {
        int g=a[d];
        a[d]=a[e];
        a[e]=g;
      }
    }
  }
}
