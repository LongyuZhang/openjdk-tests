protected static String fn_A(int a,char[] b,int c){
  char d[]=new char[c];
  int e;
  for (e=c - 1; e >= 0; e--) {
    d[e]=b[a % b.length];
    a=a / b.length - 1;
    if (a < 0) {
      break;
    }
  }
  for (e--; e >= 0; e--) {
    d[e]=' ';
  }
  return new String(d);
}
