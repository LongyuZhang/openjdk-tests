private static void fn_A(int[] a,int b,int[] c,int d) throws RuntimeException {
  if (b < 1) {
    throw new IllegalArgumentException("invalid input length: " + b);
  }
  if (b > a.length) {
    throw new IllegalArgumentException("input length out of bound: " + b + " > "+ a.length);
  }
  if (b * 2 > c.length) {
    throw new IllegalArgumentException("input length out of bound: " + (b * 2) + " > "+ c.length);
  }
  if (d < 1) {
    throw new IllegalArgumentException("invalid input length: " + d);
  }
  if (d > c.length) {
    throw new IllegalArgumentException("input length out of bound: " + b + " > "+ c.length);
  }
}
