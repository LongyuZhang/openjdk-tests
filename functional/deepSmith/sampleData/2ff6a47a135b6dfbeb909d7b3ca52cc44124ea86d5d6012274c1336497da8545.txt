private static String fn_A(String a){
  String b=new String("#!/bin/bash -e\n " + "execID=$1\n" + "current_dir=`dirname $0` \n"+ "\n"+ "pid=`cat $current_dir/compss_$execID.pid` \n"+ "kill $pid");
  return b;
}
