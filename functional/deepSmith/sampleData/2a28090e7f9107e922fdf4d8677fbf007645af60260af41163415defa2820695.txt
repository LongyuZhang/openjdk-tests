private static double fn_A(double a,double b,double c){
  double d=a / c;
  double e=b / d;
  double[] f={500.0,200.0,100.0,50.0,20.0,10.0,5.0,2.0,1.0,0.5,0.25,0.1};
  for (int g=0; g < f.length; g++) {
    if (e > f[g]) {
      double h=e % f[g];
      return e - h;
    }
  }
  return e;
}
