static public int fn_A(int[] a){
  if ((a == null) || (a.length == 0)) {
    throw new IllegalArgumentException();
  }
  int b=Integer.MAX_VALUE;
  for (int c=0; c < a.length; c++) {
    if (a[c] < b) {
      b=a[c];
    }
  }
  return b;
}
