static void fn_A(int a){
  Long[] b=new Long[a];
  for (int c=0; c < b.length; ++c)   b[c]=new Long(c);
  int d=0;
  for (int c=0; c < b.length; ++c)   d+=(int)(b[c].longValue() + c);
  if (d == 0)   System.out.println("Useless number = " + d);
  b=null;
}
