private static void fn_A(double[] a,int b,int c,int d,int e){
  for (int f=0, g=c; f < e; f++, g+=d) {
    for (int h=0, i=b + f; h < e; h++, i+=d) {
      double j=a[g + h];
      a[g + h]=a[i];
      a[i]=j;
    }
  }
}
