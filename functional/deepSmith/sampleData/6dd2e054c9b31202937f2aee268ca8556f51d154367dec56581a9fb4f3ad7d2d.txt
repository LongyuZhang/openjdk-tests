public static int fn_A(double[] a,double b){
  double c=-1;
  int d=-1;
  for (int e=0; e < a.length; e++) {
    double f=Math.abs(a[e] - b);
    if ((f <= c && b >= a[e]) || c < 0 || d < 0) {
      c=f;
      d=e;
    }
  }
  return d;
}
