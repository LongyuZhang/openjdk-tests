public static long fn_A(long a,long b){
  if (b >= 0L) {
    if (a >= 0L) {
      return a / b;
    }
    long c=((a >>> 1) / b) << 1;
    a-=c * b;
    if (a < 0L || a >= b) {
      c++;
    }
    return c;
  }
  return a >= 0L || a < b ? 0L : 1L;
}
