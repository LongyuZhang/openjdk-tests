private static int[] fn_A(char[] a){
  int b=2;
  for (int c=0; c < a.length; c++) {
    if (a[c] == '\n') {
      b++;
    }
  }
  int[] d=new int[b];
  int e=0;
  d[e++]=0;
  d[e++]=0;
  for (int c=0; c < a.length; c++) {
    if (a[c] == '\n') {
      d[e++]=c + 1;
    }
  }
  return d;
}
