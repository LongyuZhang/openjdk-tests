private static double[] fn_A(double[] a){
  double b=0;
  for (int c=0; c < a.length; c++) {
    if (Math.abs(a[c]) > b) {
      b=Math.abs(a[c]);
    }
  }
  for (int c=0; c < a.length; c++) {
    a[c]=a[c] / b;
  }
  return a;
}
