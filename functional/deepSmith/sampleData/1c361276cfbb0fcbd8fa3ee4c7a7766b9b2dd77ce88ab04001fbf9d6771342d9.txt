public static int fn_A(int[] a,int[] b){
  if (a.length != b.length)   throw new IllegalArgumentException(String.format("Arrays have different length: x[%d], y[%d]",a.length,b.length));
  int c=0;
  for (int d=0; d < a.length; d++) {
    if (a[d] != b[d])     c++;
  }
  return c;
}
