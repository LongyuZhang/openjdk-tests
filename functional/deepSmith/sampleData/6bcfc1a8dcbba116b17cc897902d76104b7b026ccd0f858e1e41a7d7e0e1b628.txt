public static float[][] fn_A(float[][] a,int b,int c){
  int d=1 + c - b;
  float[][] e=new float[a.length][d];
  int f=0;
  for (int g=b; g <= c; g++) {
    for (int h=0; h < a.length; h++) {
      e[h][f]=a[h][g];
    }
    f++;
  }
  return e;
}
