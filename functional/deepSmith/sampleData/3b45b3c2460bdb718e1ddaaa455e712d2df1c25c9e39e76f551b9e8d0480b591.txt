static long[] fn_A(long a,long b){
  System.out.println("IntMath.longOperTest");
  long[] c=new long[10];
  c[0]=a + b;
  c[1]=a - b;
  c[2]=a * b;
  c[3]=a * a;
  c[4]=a / b;
  c[5]=a % -b;
  c[6]=a & b;
  c[7]=a | b;
  c[8]=a ^ b;
  c[9]=a + ((((((((a + b) - b) * b) / b) % b) & b) | b) ^ b);
  return c;
}
