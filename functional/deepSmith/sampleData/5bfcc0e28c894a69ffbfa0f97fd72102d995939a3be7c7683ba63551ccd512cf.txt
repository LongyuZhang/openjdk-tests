public static int[] fn_A(int[] a,boolean b){
  boolean c=false;
  for (int d=0; d < a.length; ++d) {
    if (a[d] == 0) {
      if (c) {
        a[d]=(b ? -2 : -1);
      }
 else {
        a[d]=(b ? -1 : -2);
        c=true;
      }
    }
  }
  return a;
}
