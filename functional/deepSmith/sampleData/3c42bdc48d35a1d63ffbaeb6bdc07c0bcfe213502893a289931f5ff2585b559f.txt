public static int fn_A(char a){
  if (a >= '0' && a <= '9')   return (int)(a - '0');
  if (a >= 'a' && a <= 'f')   return (10 + (int)(a - 'a'));
  if (a >= 'A' && a <= 'F')   return (10 + (int)(a - 'A'));
  return 0;
}
