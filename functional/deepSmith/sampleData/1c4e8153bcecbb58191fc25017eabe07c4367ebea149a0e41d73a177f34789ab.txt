public static long fn_A(int a,int b){
  long c=1;
  if (b > a / 2) {
    b=a - b;
  }
  for (int d=0; d < b; d++) {
    long e=c;
    c=c * (a - d) / (d + 1);
    if (e > c) {
      throw new StackOverflowError("long range exceeded");
    }
  }
  return c;
}
