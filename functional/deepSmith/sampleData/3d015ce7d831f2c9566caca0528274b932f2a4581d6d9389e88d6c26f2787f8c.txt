static int fn_A(int a[]){
  if (a.length == 3) {
    int b=a[0];
    int c=a[1];
    int d=a[2];
    if (b < c) {
      return (b < d ? b : d);
    }
 else {
      return (c < d ? c : d);
    }
  }
  int e=a[0];
  for (int f=1; f < a.length; f++) {
    if (a[f] < e)     e=a[f];
  }
  return e;
}
