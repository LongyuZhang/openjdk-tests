public static void fn_A(int[] a,int[] b,int c){
  int d=-1;
  for (int e=a.length - 1; e >= 0; e--) {
    if (a[e] == c) {
      d=e;
    }
    if ((d == -1 || b[e] < d) && (b[e] != -1)) {
      b[e]=d;
    }
  }
}
