public static double[] fn_A(float[][] a,float[][] b){
  int c=a[0].length;
  int d=a.length;
  int e=d * c;
  double[] f=new double[e];
  int g=0;
  for (int h=0; h < c; h++) {
    for (int i=0; i < d; i++) {
      f[g++]=a[i][h] / b[i][h];
    }
  }
  return f;
}
