public static int fn_A(Float[] a,double b){
  int c=0;
  double d=Math.abs(b - a[0]);
  for (int e=1; e < a.length; e++) {
    double f=Math.abs(b - a[e]);
    if (f < d) {
      d=f;
      c=e;
    }
  }
  return c;
}
