static int fn_A(char[] a,int b,char[] c,int d,int e){
  int f;
  for (f=0; f < e; ++f) {
    if (('\0' == a[b + f]) && ('\0' == c[d + f])) {
      return 0;
    }
    if (a[b + f] < c[d + f]) {
      return 1;
    }
 else     if (a[b + f] > c[d + f]) {
      return -1;
    }
  }
  return 0;
}
