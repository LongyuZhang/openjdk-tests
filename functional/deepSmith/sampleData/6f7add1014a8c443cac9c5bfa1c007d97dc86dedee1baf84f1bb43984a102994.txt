private static void fn_A(double[] a,double b,double[] c,double[] d,double[] e){
  double f=(1 - b) * (1 - b);
  double g=2 * b * (1 - b);
  double h=b * b;
  a[0]=f * c[0] + g * d[0] + h * e[0];
  a[1]=f * c[1] + g * d[1] + h * e[1];
}
