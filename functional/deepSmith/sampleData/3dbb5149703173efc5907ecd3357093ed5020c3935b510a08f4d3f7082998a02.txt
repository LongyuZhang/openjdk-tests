static boolean fn_A(String a,String b){
  char[] c=a.toCharArray();
  char[] d=b.toCharArray();
  if (c.length != d.length) {
    return false;
  }
  byte e=0;
  for (int f=0; f < c.length; f++) {
    e|=c[f] ^ d[f];
  }
  return e == 0;
}
