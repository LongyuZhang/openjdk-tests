public static double fn_A(double[] a,double[] b){
  double c=0;
  for (int d=0; d < a.length; d++) {
    if (a[d] != 0 && b[d] != 0) {
      double e=a[d] + b[d];
      c+=(e / 2) * Math.log(e / (2 * Math.sqrt(a[d] * b[d])));
    }
  }
  return c;
}
