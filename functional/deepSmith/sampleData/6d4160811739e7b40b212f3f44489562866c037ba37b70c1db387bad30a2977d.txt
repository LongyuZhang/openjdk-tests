public static void main(String a[]) throws Exception {
  Map b[]={new HashMap(),new TreeMap()};
  for (int c=0; c < b.length; c++) {
    b[c].put("bananas",null);
    if (!b[c].keySet().remove("bananas"))     throw new Exception("Yes, we have no bananas: " + c);
  }
}
