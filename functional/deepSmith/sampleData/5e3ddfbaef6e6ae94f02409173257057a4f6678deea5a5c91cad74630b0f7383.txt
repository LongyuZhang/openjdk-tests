public static String fn_A(int a,int b,int c,int d,int e){
  int f=((b - a) + (d - c)) / 2;
  int g=Math.abs((b + a) / 2 + (d + c) / 2);
  if (f + e > g) {
    return "No intersect";
  }
 else   if (f == g) {
    return "Cover";
  }
 else {
    return "intersect";
  }
}
