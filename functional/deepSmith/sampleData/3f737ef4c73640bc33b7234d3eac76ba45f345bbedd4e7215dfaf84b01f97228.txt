public static double fn_A(double[] a){
  if (a.length == 0)   return 0;
  int b=0;
  double c=0;
  for (int d=0; d < a.length; d++) {
    if (a[d] != 0) {
      c+=a[d];
      b++;
    }
  }
  return b == 0 ? 0 : c / b;
}
