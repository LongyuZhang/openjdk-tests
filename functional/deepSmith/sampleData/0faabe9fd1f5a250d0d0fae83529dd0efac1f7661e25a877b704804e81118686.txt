public static double[] fn_A(double[] a,double[] b){
  int c=a.length + b.length;
  double[] d=new double[c];
  for (int e=0; e < a.length; e++) {
    d[e]=a[e];
  }
  for (int e=a.length; e < c; e++) {
    d[e]=b[e - a.length];
  }
  return d;
}
