public static int[][] fn_A(int[][] a){
  int[][] b=new int[a.length][3];
  for (int c=0; c < a.length; c++) {
    if (c < a.length / 2) {
      b[c]=a[c];
    }
 else {
      b[c]=a[a.length - 1 - c];
    }
  }
  return b;
}
