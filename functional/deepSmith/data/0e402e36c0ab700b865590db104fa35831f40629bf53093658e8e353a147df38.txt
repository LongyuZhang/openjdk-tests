public static int[][] fn_A(double[][] a){
  int[][] b=new int[a.length][];
  for (int c=0; c < a.length; c++) {
    b[c]=new int[a[c].length];
    for (int d=0; d < a[c].length; d++)     b[c][d]=(int)a[c][d];
  }
  return b;
}
