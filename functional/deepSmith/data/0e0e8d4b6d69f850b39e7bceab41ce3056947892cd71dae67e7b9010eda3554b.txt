private static int[][] fn_A(int[] a,int b){
  int[][] c=new int[a.length / b][b];
  for (int d=0; d < a.length / b; d++) {
    for (int e=0; e < b; e++) {
      c[d][e]=a[d * b + e];
    }
  }
  return c;
}
