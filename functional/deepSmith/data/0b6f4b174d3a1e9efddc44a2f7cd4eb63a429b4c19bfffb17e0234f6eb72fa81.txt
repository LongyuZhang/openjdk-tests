public static double fn_A(double[][] a){
  double b=0;
  for (int c=0; c < a.length; c++) {
    for (int d=0; d < a[0].length; d++) {
      b+=a[c][d] / (1 + Math.abs(c - d));
    }
  }
  return b;
}
