public static boolean fn_A(int[] a){
  for (int b=0; b < a.length; ++b) {
    for (int c=b + 1; c < a.length; ++c) {
      if (a[c] < a[b])       continue;
      for (int d=c + 1; d < a.length; ++d) {
        if (a[d] > a[c])         return true;
      }
    }
  }
  return false;
}
