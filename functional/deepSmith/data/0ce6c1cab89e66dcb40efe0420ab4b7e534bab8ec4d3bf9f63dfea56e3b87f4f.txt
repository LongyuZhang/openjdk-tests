public static void fn_A(Thread[] a){
  for (int b=0; b < a.length; b++) {
    a[b].start();
  }
  for (int b=0; b < a.length; b++) {
    try {
      a[b].join();
    }
 catch (    InterruptedException c) {
    }
  }
}
