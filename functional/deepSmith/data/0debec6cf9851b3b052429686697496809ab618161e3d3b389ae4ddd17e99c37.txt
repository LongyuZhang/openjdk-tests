private static void fn_A(double a[],double b[],double c[],int d,int e,int f){
  int g, h, i;
  for (g=0; g < d; ++g) {
    for (h=0; h < f; ++h) {
      double j=0;
      for (i=0; i < e; ++i) {
        j+=a[g * e + i] * b[i * f + h];
      }
      c[g * f + h]=j;
    }
  }
}
