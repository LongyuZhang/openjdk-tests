public static void fn_A(String[] a){
  for (int b=0; b < a.length; b++) {
    if (("ssh-rsa".equals(a[b]) == false) && ("ssh-dss".equals(a[b]) == false))     throw new IllegalArgumentException("Unknown server host key algorithm '" + a[b] + "'");
  }
}
