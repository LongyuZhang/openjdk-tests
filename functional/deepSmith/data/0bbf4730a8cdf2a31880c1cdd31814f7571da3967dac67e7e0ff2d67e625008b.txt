public static int[] fn_A(final double[] a){
  if (a == null)   return null;
  final int[] b=new int[a.length];
  for (int c=0; c < a.length; c++) {
    b[c]=Math.toIntExact(Math.round(a[c]));
  }
  return b;
}
