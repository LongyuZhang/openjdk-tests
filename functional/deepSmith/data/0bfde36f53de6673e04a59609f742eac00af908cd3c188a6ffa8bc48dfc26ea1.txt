public static float[][] fn_A(int[] a,int b,int c){
  float[][] d=new float[c][b];
  for (int e=0, f=0; e < c; e++) {
    for (int g=0; g < b; g++, f++) {
      d[e][g]=(float)(((a[f] >> 16) & 0xff) * 0.2126 + ((a[f] >> 8) & 0xff) * 0.7152 + (a[f] & 0xff) * 0.0722 - 128.0);
    }
  }
  return d;
}
