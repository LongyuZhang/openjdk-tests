public static int fn_A(int[] a,int b){
  if (b < a[0]) {
    return 0;
  }
  if (b > a[a.length - 1]) {
    return a.length;
  }
  for (int c=0; c < a.length; c++) {
    if (a[c] == b) {
      return c;
    }
    if (b > a[c] && b < a[c + 1]) {
      return c + 1;
    }
  }
  return 0;
}
