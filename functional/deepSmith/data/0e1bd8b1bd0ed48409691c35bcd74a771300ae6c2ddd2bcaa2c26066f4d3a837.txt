private static Thread fn_A(){
  return new Thread(){
    public void run(){
      try {
        sleep(2000);
      }
 catch (      InterruptedException a) {
        return;
      }
      System.err.println(" (might take a few minutes for large data sets)");
    }
  }
;
}
