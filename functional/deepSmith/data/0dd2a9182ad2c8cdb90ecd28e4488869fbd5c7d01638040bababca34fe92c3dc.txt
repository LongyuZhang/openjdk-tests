private static int fn_A(final byte[] a){
  int b=1;
  int c=0;
  for (int d=0; d < a.length; d++) {
    final int e=a[d] >= 0 ? a[d] : (a[d] + 256);
    b=(b + e) % 65521;
    c=(c + b) % 65521;
  }
  return (c << 16) + b;
}
