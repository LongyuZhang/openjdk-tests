private static float[][] fn_A(int a,int b){
  float[][] c=new float[a][b];
  for (int d=0; d < a; d++) {
    for (int e=0; e < b; e++) {
      c[d][e]=(float)Math.cos((d + 0.5f) * (e + 16) * Math.PI / 32);
    }
  }
  return c;
}
