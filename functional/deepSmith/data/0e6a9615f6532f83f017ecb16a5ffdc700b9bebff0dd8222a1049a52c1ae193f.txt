public static double fn_A(double[] a,double[] b){
  if (a.length != b.length)   throw new RuntimeException("Illegal vector dimensions.");
  double c=0.0;
  for (int d=0; d < a.length; d++)   c+=a[d] * b[d];
  return c;
}
