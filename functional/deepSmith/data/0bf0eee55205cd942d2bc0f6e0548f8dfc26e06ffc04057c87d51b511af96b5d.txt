public static float fn_A(float[] a){
  float b=0;
  float c=0;
  for (int d=0; d < a.length; d++) {
    c+=(float)a[d] / (float)a.length;
  }
  for (int d=0; d < a.length; d++) {
    b+=((a[d] - c) * (a[d] - c)) / (float)a.length;
  }
  return b;
}
